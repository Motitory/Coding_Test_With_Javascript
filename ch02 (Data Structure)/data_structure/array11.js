// indexOf() : Finds a specific element in an array and returns its index. If it doesn't exist, it returns -1.
let arr = [7, 3, 5, 6, 6, 2, 1];

console.log(arr.indexOf(5));
console.log(arr.indexOf(6));
console.log(arr.indexOf(8));
